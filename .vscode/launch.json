{
    "configurations": [
        
        {
            "name": "Kubernetes: Run/Debug - dockerfile",
            "type": "cloudcode.kubernetes",
            "request": "launch",
            "skaffoldConfig": "${workspaceFolder}/java/java-hello-world/skaffold.yaml",
            "profile": "dockerfile",
            "watch": true,
            "cleanUp": true,
            "portForward": true
        }
    ]
}